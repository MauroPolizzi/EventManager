@model IEnumerable<PP.IServicio.Configuracion.ConfiguracionDto>

@{
    ViewBag.Title = "Index";
}
<br />
<br />
<br />
<br />
<br />
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Publica)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MostrarCantidadEntradas)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Publica)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MostrarCantidadEntradas)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |

            </td>
        </tr>
    }

</table>
